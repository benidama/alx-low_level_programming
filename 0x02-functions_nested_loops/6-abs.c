#include "main.h"

/**
 * _abs - function tha computes the absolute value of an integer
 * @u: the int to check
 * Return: the absolute value of int
 */

int _abs(int u)
{
if (u >= 0)
{
return (u);
}
return (-u);
}

